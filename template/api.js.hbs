{{#if importTpl }}
{{{ importTpl }}}
{{else}}
const axios = require('../axios');
{{/if}}
const config = require('./config');

module.exports = {
{{#list apis }}
    {{#if ../apiTpl}}
    {{{ apiTplFormat ../apiTpl apiName }}}
    {{else}}
    {{ apiName }}: param => axios(Object.assign(config.{{ apiName }}, param)),
    {{/if}}
{{/list}}
};
